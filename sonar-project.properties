# Configuración de SonarCloud
# Reemplaza YOUR_ORG con tu organización de SonarCloud
# Reemplaza YOUR_PROJECT_KEY con la clave de tu proyecto

sonar.organization=jose27luis
sonar.projectKey=Jose27luis_red-social
sonar.projectName=Red Academica UNAMAD

# Fuentes del proyecto
sonar.sources=backend/src,frontend/app,frontend/components,frontend/lib
sonar.tests=backend/src,frontend
sonar.test.inclusions=**/*.spec.ts,**/*.spec.tsx,**/*.test.ts,**/*.test.tsx

# Exclusiones
sonar.exclusions=**/node_modules/**,**/dist/**,**/.next/**,**/coverage/**,**/*.config.js,**/*.config.ts

# Cobertura de código
sonar.javascript.lcov.reportPaths=backend/coverage/lcov.info,frontend/coverage/lcov.info
sonar.typescript.lcov.reportPaths=backend/coverage/lcov.info,frontend/coverage/lcov.info

# Codificación
sonar.sourceEncoding=UTF-8

# Excluir regla de top-level await (NestJS usa CommonJS que no lo soporta)
sonar.issue.ignore.multicriteria=e1
sonar.issue.ignore.multicriteria.e1.ruleKey=typescript:S6544
sonar.issue.ignore.multicriteria.e1.resourceKey=backend/src/main.ts
